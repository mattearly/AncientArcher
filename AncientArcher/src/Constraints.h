#pragma once

static float world_width = 40.0f;
static float world_height = 40.0f;
static float stat_divisor = 40.0;  //this controls how much effect the player stats have. higher = less effect (hence divisor)

class Constraints {
public:
  Constraints();
  ~Constraints();
};

